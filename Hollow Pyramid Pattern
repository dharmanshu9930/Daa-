#include <iostream>
using namespace std;
int main(){
int i,j,k;
for(i=1;i<=6;i++)
    { 
    for(j=5;j>=i;j--)
        {cout<<" ";}
    for(k=1;k<=i;k++)
    {
        if(k==1 || i==k || i==6)
        {cout<<"* ";}
        else
        {cout<<"  ";}
    }    
    cout<<"\n";
    }
return 0;
}
